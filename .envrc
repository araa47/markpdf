#!/usr/bin/env bash
set -e

uv sync --all-extras --dev

# Install prek hooks (faster pre-commit alternative)
# NOTE: feel free to enable this line, once you git init your repo since prek requires a git repo to work
#uv run prek install

# Add the virtual environment's bin directory to PATH
PATH_add .venv/bin
